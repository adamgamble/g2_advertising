!!!
%html
  %head
    %title G2Advertising Admin Panel
    = stylesheet_link_tag    "application", :media => "all"
    = javascript_include_tag "application"
    = csrf_meta_tags
  %body
    .navbar.navbar-static-top
      .navbar-inner
        .container
          %a.brand{:href => "/"}
            G2 Advertising Admin Panel
          %ul.nav
            %li= link_to "Dashboard", "/"
            %li= link_to "Admin Users", admin_admin_users_path
            %li= link_to "Store Management", "#"
            %li= link_to "Notification Preferences", notification_preferences_path
          %ul.nav.pull-right
            -if current_admin_user
              %li= link_to "Logout", destroy_admin_user_session_path, :method => :delete
            -else
              %li= link_to "Login", new_admin_user_session_path
    .container
      -flash.keys.each do |key|
        .alert
          %button.close{:type => "button", "data-dismiss" => "alert"} x
          =flash[key]
      = yield
